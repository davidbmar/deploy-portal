# Deploy Portal Upstream Configuration
# This file is managed in the deploy-portal repository
# Deploy to: /etc/nginx/conf.d/system-upstreams/deploy-portal.conf

upstream deploy_portal {
    server 127.0.0.1:5000;
}
